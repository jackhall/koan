//! The scheduler's tests: the drain driven by native steps, with no dispatch layer present.

mod boundary;
mod calls;
mod continuation;
mod delivery;
mod drain;
mod native;
mod placement;
mod submissions;
mod subtree;
mod tail;
