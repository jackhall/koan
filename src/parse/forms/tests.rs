//! Form-table tests: the table's own shape, the binder facts each entry carries, and the lazy slots
//! it stamps — each pinned against the live builtin registration table.

mod binder;
mod lazy;
mod table;
