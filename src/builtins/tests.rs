//! Registration-door tests: the checks every builtin signature passes through, and the
//! cross-surface kind rule the builtins that declare a value's type share.

mod carrier_union;
mod unsaturated_constructor;
