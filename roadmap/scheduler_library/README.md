# Scheduler library

The extraction of Koan's runtime substrate — scheduler, region memory, witnessed
carriers — into one Koan-agnostic library whose public surface is memory-safe by
construction. North star: [design/scheduler-library.md](../../design/scheduler-library.md).
This project's items land the consumer API and the boundary moves incrementally;
each item is sized for one PR.

## Next items

This project's items with no unshipped prerequisite — ready to start.
Regenerated by `python3 tools/doclinks.py sync-next`; do not edit by hand.

- [Publishing the workgraph crate](workgraph-extraction.md)

## Unplanned

Known gaps deliberately **not** filed as work items yet, sitting on top of the
reference-only carrier and delivery-driven frame-retention surface the
witness-hosting trio put in place.

- **`CallFrame` still strong-owns its region storage.** The scheduler already
  pins every per-node cart opaquely (`NodeFrame.cart: Rc<W::Cart>`) and, with
  frame retention, holds frame owners on its own rows (`retain`, `handoff` in
  [dep_graph.rs](../../workgraph/src/scheduler/dep_graph.rs)) — yet
  [`CallFrame`](../../src/machine/core/arena.rs) keeps its own strong
  `Rc<FrameStorage>`, the one Koan-side strong region owner in the per-call
  lifecycle. Moving the frame owner into scheduler-held per-slot state (an
  `Rc<W::Frame>` beside the cart) would leave `CallFrame` a pure semantics
  shell: scope carrier, return contract, close-owner slot. A solution here may
  also dissolve the construction-time ancestor-ownership policy —
  `CallFrame::new`'s `outer_frame` parameter, by which Koan decides which
  frames strong-own which ancestor storage: with the scheduler owning frames
  and already knowing the caller graph, the ancestor link could become
  graph-derived rather than Koan-asserted.

- **Region-purity is caller-asserted at the move-in alloc surfaces.** The
  substrate's `resident` construction path seals a value under the empty
  (pins-nothing) witness, which is sound only if the value reaches no foreign
  region. Two surfaces accept a **pre-built** value and store it erased —
  `RegionBrand::alloc_object_witnessed` and the bare `alloc_type` (both in
  [arena.rs](../../src/machine/core/arena.rs)) — so the `for<'b>` brand never
  vets the value's internal borrows; the born-pure path (`alloc` with a
  closure building from the brand) is the only one where purity is a compiler
  fact. What upholds it is a call-site convention: the
  `carrier == None ⟺ value is region-pure` match arms in
  [val_decl.rs](../../src/builtins/val_decl.rs) and
  [newtype_def.rs](../../src/builtins/newtype_def.rs) route a dep-fed value
  through `alloc_type_with` and only asserted-pure values through the bare
  arm — enforced by comment, not type. Choosing the bare arm for a
  dep-reaching value compiles clean and under-pins (a latent dangle; no
  current call site does this, and the Miri slate exercises only correct
  paths). Candidate closures, if the gap survives the witness-hosting rework:
  a negative Miri fixture pinning the precondition, obligation-naming renames
  (`alloc_type` → `alloc_type_region_pure`), or a lifetime-free owned-leaf
  carrier family that makes "reaches nothing foreign" a type fact.

