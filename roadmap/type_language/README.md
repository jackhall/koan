# Type language

Engine-level type-language substrate — how modules, signatures, functors,
deferred-return FNs, record-shaped parameter binding, and VAL-slot identity
are represented in `KType` and routed through dispatch. The substrate the
predicate-typing stages and the stdlib's functor-heavy collections both
build on.

## Next items

This project's items with no unshipped prerequisite — ready to start.
Regenerated by `python3 tools/doclinks.py sync-next`; do not edit by hand.

- [Constructing circular values](circular-value-construction.md)
- [Constructors as first-class function values](constructor-as-first-class-function.md)
- [Function-typed return annotations](function-typed-return-annotations.md)
- [RECURSIVE TYPES group finalize ordering](recursive-group-finalize-ordering.md)

## Unplanned work

Known gaps adjacent to this project's items, recorded so they aren't lost. **To start one,
first promote it into its own `roadmap/type_language/<item>.md` file** with `**Problem.**` /
`**Acceptance criteria.**` / `## Dependencies` sections (see the documentation skill's
"When adding a new roadmap item"), then replace its prose entry here with a link. These
paragraphs name the gap, not the fix.

- **A UNION cannot reference a later group member.** The
  [tutorial's newtypes chapter](../../tutorial/08-newtypes.md) declares
  `RECURSIVE TYPES Listy = (UNION Chain = (Link :Cell Done :Null) NEWTYPE Cell = :{head :Number, tail :Chain})`,
  and the run fails with `UNION \`Chain\` schema references unsealed type \`Cell\``. A
  `RECURSIVE TYPES` group exists precisely so its members may reference each other regardless of
  source order, so a forward reference from a UNION arm to a sibling NEWTYPE should seal, not
  error. Adjacent to
  [RECURSIVE TYPES group finalize ordering](recursive-group-finalize-ordering.md) — that item's
  fault is a *panic* on a cross-binder qualified sigil, this one a typed error on a plain forward
  reference — and the two may share a root in group finalize ordering. The snippet is the one
  `python3 tools/verify_snippets.py` failure; `tools/verify.sh` does not run that script, so a
  tutorial snippet can rot without any gate noticing.
- **A parameter's token class is checked at the call, not the definition.** A parameter's name
  picks its universe: a Type token names a type, a value token names a value
  ([design/typing/tokens.md § Token class is a binding rule](../../design/typing/tokens.md)). A
  parameter whose name contradicts what its slot admits — `FN (MAKETREE elt :Type)`, a
  value-token name on a type-denoting slot — defines without complaint, and every call then fails
  when the binding maps refuse the crossing. The diagnostic names the call site, not the
  declaration that is actually wrong. A definition-time check comparing each parameter's token
  class against its slot's channel would report the mistake where it was made.

