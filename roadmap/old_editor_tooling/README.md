# Editor tooling

> **Stale as work, kept as requirements.** Written against the old runtime,
> now behind `pending_rewrite`; the items below record what the language needs
> and are retired by the [rewrite](../rewrite/README.md) as it meets them.

Surface that lets external tools — editors, debuggers, build systems — see
intermediate Koan state. The build-time / run-time scheduler split is the
foundation.

## Items

Every requirements doc in this retired project.

- [Continue-on-error for the REPL and batch mode](continue-on-error.md)
- [Two-phase execution: build-time with pegged inputs, run-time resume](two-phase-execution.md)
